---
webapp:
  release_dir: ""
  installation_path: "/home/{{ ansible_user_id }}"
  apps:
    - name: app
      directories:
        - /mnt/app/s3
        - /mnt/app/s3fs
      repo:
        url: "https://github.com/Aplyca/ansible-role-webapp.git"        
