---
webapp:
  repo:
    url: "https://github.com/Aplyca/ansible-role-webapp.git"
    version: master
  name: "webapp"
  installation_path: /var/www
  ACL_dirs: []
  app_dirs: []
  storage: []
  links_to_storage: []
  internal_links: []
  config: []
  install_commands: []

php:
  opcache:
    enable_file_override: 1
    interned_strings_buffer: 16
    revalidate_path: 1
    revalidate_freq: 60
  core:
    realpath_cache_size: 2046K
    realpath_cache_ttl: 3600

apache:
  virtualhosts:
    - ServerName: "{{ webapp['name'] | lower }}.com"
      ServerAlias:
        - "*.{{ webapp['name'] | lower }}.com"
      DocumentRoot: "/var/www/{{ webapp['name'] }}/current/tests/webapp/web"
      IncludeOptional:
        - "/var/www/{{ webapp['name'] }}/current/tests/webapp/include.conf"
